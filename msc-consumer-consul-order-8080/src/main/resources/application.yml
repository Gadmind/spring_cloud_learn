server:
  port: 8080
spring:
  application:
    name: msc-consumer-order
  cloud:
    #Consul服务配置中心
    consul:
      host: localhost
      port: 8500
      discovery:
        #hostname: 127.0.0.1
        service-name: ${spring.application.name}